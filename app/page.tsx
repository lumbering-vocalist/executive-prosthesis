/*
 * T1 placeholder. The real home is the stream (§5.5) and its frame lands at
 * T4 (proto-stream, D9). Until then this renders the custody promise so the
 * deployed scaffold is a real page, not a framework default.
 */
export default function Home() {
  return (
    <p style={{ marginTop: "40vh", textAlign: "center" }}>
      Tell me things. I keep them, no matter what.
    </p>
  );
}
